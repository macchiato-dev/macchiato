# Archived Markdown editor example

This standalone reconstruction replaces the unavailable demo in the 2021 post.
It bundles CodeMirror and Remark without the former Next.js application. The
publisher serves it from the separate blog-example origin in an iframe whose
sandbox grants scripts and no same-origin or network capability.
