# Simple Page

This is a simple page built with Macchiato.

## How it works

The build script reads this file, parses it with
`@macchiato-dev/parse-prose`, renders it with
`@macchiato-dev/render-prose`, and writes HTML to stdout.
