// The Resources frontend consumes a prebuilt machine module so its controller
// remains an application integration point rather than another machine build.
export { default } from "../../../dev/wasm-web-machine/dist/module/wasm-web-machine.js";
