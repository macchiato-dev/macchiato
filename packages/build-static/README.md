# @macchiato-dev/build-static

Provides a partial VDOM for the renderers on the server side and renders it
statically. It provides a `document` that has `createElement` and can be
used to construct a DOM as well as methods and properties used by the
renderers. It encompasses server-side generation (SSG), server-side
rendering (SSR), and testing.

